

## Plan: Increase Logo Size

Change the logo `className` from `h-8` to `h-10` in both `Navbar.tsx` (line 62) and `Footer.tsx` to match the visual weight of the "Request Demo" button (which has `py-2.5` + text, roughly 40px tall = `h-10`).

### Files
1. **`src/components/layout/Navbar.tsx`** — Change `h-8` → `h-10` on the logo img
2. **`src/components/layout/Footer.tsx`** — Same change for consistency

